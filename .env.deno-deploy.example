# ============================================================
# Deno Deploy 環境變數
# ============================================================
# 用於 Web App (main.tsx)
# 在 Deno Deploy Dashboard → Settings → Environment Variables 設定
# ============================================================

# --- 必填 ---

# Turso Database (讀取資料)
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# ChromaDB (向量搜尋)
# 指向 Fly.io 上的 ChromaDB
CHROMA_HOST=https://astrogroot-chromadb.fly.dev
CHROMA_AUTH_TOKEN=your-chroma-auth-token

# --- 選填 ---

# NASA API (如果要在 web app 直接呼叫 NASA API)
# NASA_API_KEY=your-nasa-key
