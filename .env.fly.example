# ============================================================
# Fly.io 環境變數
# ============================================================
# 包含 ChromaDB 和 Crawler 兩個服務的設定
# 使用 fly secrets set 設定
# ============================================================

# ============================================================
# ChromaDB 服務 (fly.chromadb.toml)
# ============================================================
# 設定方式: fly secrets set --config fly.chromadb.toml

# ChromaDB 伺服器認證 (必填)
# 這是 ChromaDB 伺服器端的認證 token
# Crawler 和 Deno Deploy 的 CHROMA_AUTH_TOKEN 要與此一致
CHROMA_SERVER_AUTH_CREDENTIALS=your-secure-random-token

# ============================================================
# Crawler 服務 (fly.toml)
# ============================================================
# 設定方式: fly secrets set --config fly.toml

# --- 必填 ---

# Turso Database (寫入資料)
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# ChromaDB (寫入向量)
# 指向同在 Fly.io 的 ChromaDB 服務
CHROMA_HOST=https://astrogroot-chromadb.fly.dev
CHROMA_AUTH_TOKEN=your-secure-random-token

# Anthropic API (AI 摘要和翻譯)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# --- 選填 ---

# 資料來源 API Keys
NASA_API_KEY=your-nasa-key
YOUTUBE_API_KEY=your-youtube-key

# 爬蟲設定 (已有預設值，可不設)
# CRAWLER_INTERVAL_HOURS=24
# MAX_ITEMS_PER_SOURCE=10
# ANTHROPIC_MODEL=claude-haiku-4-5
